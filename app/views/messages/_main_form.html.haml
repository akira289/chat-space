.main_form
  = form_for [@group, @message] do |f|
    = f.text_field :body, placeholder: "type a message", class: "main_form__input"
    = f.label :image, class: "main_form__image" do
      = icon('fas', 'image')
      = f.file_field :image, class: "main_form__image--hidden"
    = f.submit "Send", class: "main_form__btn"
