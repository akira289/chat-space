.main_form
  = form_for [@group, @message] do |f|
    .main_form__input_area
      = f.text_field :body, placeholder: "type a message", class: "main_form__input_text"
      = f.label :image do
        = icon('fas', 'image', class: "main_form__icon")
        = f.file_field :image, class: "main_form__icon--hidden"
      = f.submit "Send", class: "main_form__btn"
